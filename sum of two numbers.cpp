#include<iostream>
using namespace std;
int main() //this code is not finished
{
	int a[10], nums[10]={2,7,11,15}, len = sizeof(nums)/sizeof(nums[0]), target = 9;
	
	cout<<len;
//	cout<<nums.size();
	
/*	for(int i=0;i<len;i++)
	{
		if(nums[i]+nums[i+1]==target)
		{
			a.push_back(i);
			a.push_back(i+1);
		}
	}
	
	return a;*/
	
}
